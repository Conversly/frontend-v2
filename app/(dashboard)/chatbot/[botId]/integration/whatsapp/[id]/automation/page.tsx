export default function WhatsAppAutomationPage() {
    return (
        <div>
            <h1>we recommend autmation here. user can able to create an calls like if he ask then this and then this basically without chatbot user query  will resolve
            </h1>
        </div>
    );
}